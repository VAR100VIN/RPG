{% extends 'base.html.twig' %}

{% block title %}Competences index{% endblock %}

{% block body %}
    <h1>Competences index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Decription</th>
                <th>Degat</th>
                <th>Vitesse</th>
                <th>Portee</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for competence in competences %}
            <tr>
                <td>{{ competence.id }}</td>
                <td>{{ competence.nom }}</td>
                <td>{{ competence.decription }}</td>
                <td>{{ competence.degat }}</td>
                <td>{{ competence.vitesse }}</td>
                <td>{{ competence.portee }}</td>
                <td>
                    <a href="{{ path('app_competences_show', {'id': competence.id}) }}">show</a>
                    <a href="{{ path('app_competences_edit', {'id': competence.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_competences_new') }}">Créer</a>
{% endblock %}
